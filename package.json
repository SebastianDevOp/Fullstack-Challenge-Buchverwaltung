{
  "name": "book-manager",
  "private": true,
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "setup": "test -f packages/database/.env || cp packages/database/.env.example packages/database/.env && test -f packages/web/.env || cp packages/web/.env.example packages/web/.env && test -f packages/mcp/.env || cp packages/mcp/.env.example packages/mcp/.env && pnpm install && pnpm run db:start && pnpm --filter @book-manager/database migrate && pnpm --filter @book-manager/database seed",
    "dev": "pnpm --filter @book-manager/web dev",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "pnpm -r run typecheck",
    "clean": "docker compose down && rm -rf node_modules packages/*/node_modules packages/*/.next packages/*/dist packages/*/.turbo"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.14",
    "tsx": "4.21.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@prefecthq/fastmcp-ts": "^1.2.1",
    "@tailwindcss/postcss": "^4.3.0",
    "postcss": "^8.5.15",
    "sonner": "^2.0.7",
    "tailwindcss": "^4.3.0",
    "use-debounce": "^10.1.1",
    "zod": "^4.4.3"
  }
}
